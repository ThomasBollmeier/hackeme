(define my-answer 42) ; the answer to everything
(if #true
    result
    (my-func first-arg result))
(define (this-is-a-function a-param)
    (define local-var 23)
    (define (helper a-param)
        (define ego "Thomas")
        (do-something))
    (helper a-param))
(define (writeln words*)
    (do-stuff))
(define (even? n)
    #false)
'((this-is-a-function-call 23) 1 2 3 (4 5 (7 8 9)))
(+ 41 1)
(define blank "")
(writeln "Say \"Hello\" to the world!")
